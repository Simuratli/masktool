export interface ProgressBarPropTypes {
    bgcolor:string;
    completed:number,
}

export interface FillerStylesTypes{
    height: string;
    width: string;
    backgroundColor: string;
    transition: string;
    borderRadius: string;
}